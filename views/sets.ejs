<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/css/main.css">
    <link href="https://cdn.jsdelivr.net/npm/daisyui@4.7.2/dist/full.min.css" rel="stylesheet" type="text/css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Lego Collection - Sets</title>
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar', { page: '/lego/sets' }) %>

    <div class="container mx-auto px-4 py-20 text-center">
        <h1 class="text-6xl font-bold">Lego Sets Collection</h1>
        <p class="text-xl mt-5 mb-10">Browse through our collection of Lego sets.</p>
        <div class="flex justify-center mb-5">
            <a href="/lego/sets?theme=Classic%20Town" class="btn btn-primary mr-3">Classic Town</a>
            <a href="/lego/sets?theme=Icons" class="btn btn-primary mr-3">Icons</a>
            <a href="/lego/sets?theme=Books" class="btn btn-primary">Books</a>
        </div>
        
        <table class="table-auto">
            <thead>
                <tr>
                    <th class="px-4 py-2">Image</th>
                    <th class="px-4 py-2">Name</th>
                    <th class="px-4 py-2">Theme</th>
                    <th class="px-4 py-2">Year</th>
                    <th class="px-4 py-2">Parts</th>
                    <th class="px-4 py-2">Details</th>
                </tr>
            </thead>
            <tbody>
                <% sets.forEach(set => { %>
                    <tr>
                        <td class="px-4 py-2"><img src="<%= set.img_url %>" alt="Set Image" style="width:100px;"></td>
                        <td class="px-4 py-2"><%= set.name %></td>
                        <td class="px-4 py-2"><a href="/lego/sets?theme=<%= set.theme %>" class="btn"><%= set.theme %></a></td>
                        <td class="px-4 py-2"><%= set.year %></td>
                        <td class="px-4 py-2"><%= set.num_parts %></td>
                        <td class="px-4 py-2"><a href="/lego/sets/<%= set.set_num %>" class="btn">Details</a></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
        
        
    </div>
</body>
</html>
